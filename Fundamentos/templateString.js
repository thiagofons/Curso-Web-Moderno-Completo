const nome = "Rebeca";
const concat = "Ola " + nome + "!";

const template = `Ola ${nome}!`;        // Limitado por crases

console.log(concat);
console.log(template);

// Expressoes dentro do template string
console.log(`1 + 1 = ${1 + 1}`);
